/*Escribir la función queContiene que reciba dos strings s1 y s2. El string s1 puede contener cualquier valor ASCII. 
El string s2 contiene únicamente letras minúsculas o mayúsculas (no debe validarse, se asegura que sólo son letras del 
alfabeto inglés). La función debe retornar cuántos caracteres de s1 son letras contenidas en s2, sin distinguir entre 
minúsculas o mayúsculas. Para más detalles ver programa debajo*/
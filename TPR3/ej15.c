/*Dado un entero que representa un número binario positivo (todos sus dígitos son cero o uno), 
imprimir por salida estándar  el equivalente decimal. (En no más de 10 líneas).
Utilizá la función getint para recibir el entero, y aplicá las operaciones matemáticas 
necesarias para obtener el decimal. ¿Cómo validarías que el número ingresado no es binario?.
*/

#include <stdio.h>
#include "../biblio/getnum.h"

int main(void){
    int a = getint("ingrese numero binario:"), ; 
    for(int i = 0; )
}